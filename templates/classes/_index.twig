{% extends '_shared/_layout' %}

{% block main %}

    <main>

{# ***** intro ***** #}
        <div>
            <h3>{{ entry.title }}</h3>
            
            {{ entry.listTagline }}
        </div>

{# ***** class-info ***** #}
        {% include "_shared/_classGrid" with {
            limit: 3,
            section: 'classes'
        } %}

    </main>
    
{% endblock %}