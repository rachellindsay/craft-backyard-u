{% extends '_shared/_layout' %}

{% block main %}

{% set buLogo = entry.buLogo.one() %}

  <body>
    {# <header class="banner">
    {% if buLogo %}
      <img
        src="{{ buLogo.getUrl() }}"
        class="logo"
        alt="Backyard University logo"
      />
    {% endif %}
    </header> #}
{# ********************** intro blurb ************************ #}
    <section class="about-text">
        <p>{{ entry.tagline }}</p>
    </section>

{# *********************** classes ***************************** #}
  
    <section class="classes">

      <h1 class="workshops">Upcoming Classes</h1>
      <div class="more-info">
        <p>
          Questions?
          <a href="mailto:backyarduniversitypdx@gmail.com" class="email"
            >Email backyarduniversitypdx@gmail.com</a
          >
        </p>
      </div>
      <div class="class-grid">
        {% include "_shared/_classGrid" with {
            limit: 10,
            section: 'classes'
        } %}
      
      </div>
    </section>

{% endblock %}

    

    